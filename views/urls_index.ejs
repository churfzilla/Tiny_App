<!DOCTYPE html>
<html lang="en">
<body>
    <p>
    <h1><%=tagline%></h1>
     <ul>
    <% urlDatabase.forEach(function(url) { %>
        <li><%= url.fullUrl %> - <%= url.shortUrl %></li>
    <% }); %>
</ul>
    </p>
</body>
</html>